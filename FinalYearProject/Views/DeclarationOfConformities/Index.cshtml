@model IEnumerable<FinalYearProject.Models.DeclarationOfConformity>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CustomerName)
        </th>
        
        <th>
            @Html.DisplayNameFor(model => model.Company)
        </th>
        
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerName)
            </td>
            
            <td>
                @Html.DisplayFor(modelItem => item.Company)
            </td>
           
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.DeclarationOfConformityID }) |
                @Html.ActionLink("Details", "Details", new { id = item.DeclarationOfConformityID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.DeclarationOfConformityID })
            </td>
        </tr>
    }

</table>